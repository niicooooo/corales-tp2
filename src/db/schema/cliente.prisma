enum ClienteRol {
    ADMINISTRADOR
    CLIENTE
}

model Cliente {
    id          String  @id @default(cuid())
    nombre      String
    
    correo      String  @unique
    telefono    String     @unique
    direccion   String   
    pedidos     Pedido[]
    
    contraseña  String 
    rol         ClienteRol

    @@map("Clientes")
}